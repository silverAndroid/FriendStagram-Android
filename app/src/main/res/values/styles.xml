<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorPrimary</item>
        <item name="colorPrimaryDark">@color/colorPrimaryDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="PicturesTheme" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/colorToolbarBlack</item>
        <item name="colorPrimaryDark">@color/colorToolbarBlackDark</item>
        <item name="colorAccent">@color/colorAccent</item>
    </style>

    <style name="AppTheme.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar"/>

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light"/>

    <style name="AppTheme.LoginButton" parent="Base.Widget.AppCompat.Button">
        <item name="colorButtonNormal">@color/colorLoginButton</item>
        <item name="android:textColor">#fff</item>
    </style>

    <style name="AppTheme.RegisterButton" parent="Base.Widget.AppCompat.Button">
        <item name="colorButtonNormal">@color/colorRegisterButton</item>
        <item name="android:textColor">#fff</item>
    </style>

    <style name="AppTheme.EditTextHint">
        <item name="colorAccent">@android:color/white</item>
        <item name="android:textColorHint">#fff</item>
        <item name="colorControlNormal">#fff</item>
        <item name="colorControlHighlight">#fff</item>
    </style>

    <style name="AppTheme.LoginEditTextHint" parent="AppTheme.EditTextHint">
        <item name="colorControlActivated">@color/colorLoginAccent</item>
    </style>

    <style name="AppTheme.RegisterEditTextHint" parent="AppTheme.EditTextHint">
        <item name="colorControlActivated">@color/colorRegisterAccent</item>
    </style>

</resources>
